<form method="post" action="{{ path('app_cours_delete', {'idCour': cour.idCour}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cour.idCour) }}">
    <button class="btn">Delete</button>
</form>
