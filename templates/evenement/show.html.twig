{% extends 'baseBack.html.twig' %}


{% block body %}
    <title>Evenement</title>
    <h1>Evenement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id Evenement</th>
                <td>{{ evenement.idEvenement }}</td>
            </tr>
            <tr>
                <th>Nom Evenement</th>
                <td>{{ evenement.nomEvenement }}</td>
            </tr>
            <tr>
                <th>Date Evenement</th>
                <td>{{ evenement.dateEvenement ? evenement.dateEvenement|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Artiste</th>
                <td>{{ evenement.Artiste }}</td>
            </tr>
            <tr>
                <th>Restaurant</th>
                <td>{{ evenement.Restaurant }}</td>
            </tr>
        </tbody>
    </table>

    <div class="row"> <a href="{{ path('app_evenement_index') }}">Retour à la liste</a> </div>

    <div class="row"> <a href="{{ path('app_evenement_edit', {'idEvenement': evenement.idEvenement}) }}">Modifier</a> </div>

    {{ include('evenement/_delete_form.html.twig') }}
{% endblock %}
