{% extends 'baseBack.html.twig' %}

{% block body %}
    <h1>Artiste</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id Artiste</th>
                <td>{{ artiste.idArtiste }}</td>
            </tr>
            <tr>
                <th>Nom Artiste</th>
                <td>{{ artiste.nomArtiste }}</td>
            </tr>
            <tr>
                <th>Type De Musique</th>
                <td>{{ artiste.typeDeMusique }}</td>
            </tr>
        </tbody>
    </table>

    <div class="row"> <a href="{{ path('app_artiste_index') }}">Retour à la liste</a> </div>

    <div class="row"> <a href="{{ path('app_artiste_edit', {'idArtiste': artiste.idArtiste}) }}">Modifier</a> </div>

    {{ include('artiste/_delete_form.html.twig') }}
{% endblock %}
