

<form method="post" action="{{ path('app_chefs_delete', {'idChef': chef.idChef}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chef.idChef) }}">
    <button class="btn"><button class="btn btn-danger btn-xs"><i class="fa fa-trash-o "></i></button></button>
</form>


