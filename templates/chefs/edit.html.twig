{% extends 'baseBack.html.twig' %}

{% block title %}Edit Chefs{% endblock %}

{% block body %}
    <h1>Edit Chefs</h1>

    {{ form_start(form,{'attr': {'novalidate': 'novalidate'}} ) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}

    <a href="{{ path('app_chefs_index') }}">back to list</a>

    {{ include('chefs/_delete_form.html.twig') }}
{% endblock %}
