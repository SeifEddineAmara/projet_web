{% extends 'baseBack.html.twig' %}

{% block title %}Commentaire{% endblock %}

{% block body %}

    <br>
    <br>
    <br>
<div style="margin-left: 250px">
    <h1 >Commentaire</h1>

    <table class="table" >
        <tbody>
            <tr>
                <th>IdCommentaire</th>
                <td>{{ commentaire.idCommentaire }}</td>
            </tr>
            <tr>
                <th>LibelleCommentaire</th>
                <td>{{ commentaire.libelleCommentaire }}</td>
            </tr>
        </tbody>
    </table>




    <a href="{{ path('app_commentaire_edit', {'idCommentaire': commentaire.idCommentaire}) }}"><button class="btn btn-success btn-xs"  style="width: 120px">Edit</button></a>
    <a href="{{ path('app_publication_index') }}" ><button class="btn btn-danger btn-xs" style="margin-left: 250px;width: 120px">Retour</button></a>

    {{ include('commentaire/_delete_form.html.twig') }}

</div>
{% endblock %}
