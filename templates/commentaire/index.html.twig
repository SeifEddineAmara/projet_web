{% extends 'baseback.html.twig' %}

{% block title %}Commentaire index{% endblock %}

{% block body %}
<br>
    <br>
    <br>
    <br>
    <br>

    <table class="table" style="margin-left: 250px;width:1000px">
        <thead>
            <tr>
                <th>IdCommentaire</th>
                <th>LibelleCommentaire</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for commentaire in commentaires %}
            <tr>
                <td>{{ commentaire.idCommentaire }}</td>
                <td>{{ commentaire.libelleCommentaire }}</td>

                <td>
                    <a href="{{ path('app_commentaire_show', {'idCommentaire': commentaire.idCommentaire}) }}"><button class="btn btn-success btn-xs"><i class="fa fa-check"></i></button></a>
                    <a href="{{ path('app_commentaire_edit', {'idCommentaire': commentaire.idCommentaire}) }}"><button class="btn btn-primary btn-xs"><i class="fa fa-pencil"></i></button></a>
                    <a href="{{ path('app_commentaire_delete', {'idCommentaire': commentaire.idCommentaire}) }}"><button class="btn btn-danger btn-xs"><i class="fa fa-trash-o "></i></button></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_commentaire_new') }}"><button class="btn btn-success btn-xs" style="margin-left: 250px;width:120px;height:35px">Commenter</button></a>
    <a href="{{ path('app_publication_index') }}" ><button class="btn btn-danger btn-xs" style="margin-left: 250px;width:120px;height:35px">Retour</button></a></td>



{% endblock %}
