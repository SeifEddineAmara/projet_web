

{% block body %}
<head>

    <style>
        /**
            Set the margins of the page to 0, so the footer and the header
            can be of the full height and width !
         **/
        @page {
            margin: 0cm 0cm;
        }
        /** Define now the real margins of every page in the PDF **/
        body {
            margin-top: 10.5cm;
            margin-left: 10cm;
            margin-right: 15cm;
            margin-bottom: 8cm;
        }
        /** Define the header rules **/
        header {
            position: fixed;
            top: 0cm;
            left: 0cm;
            right: 0cm;
            height: 10cm;
        }
        /** Define the footer rules **/
        footer {
            position: fixed;
            bottom: 0cm;
            left: 0cm;
            right: 0cm;
            height: 2cm;
        }
    </style>
</head>
<body>
<!-- Define header and footer blocks before your content -->
<header>

    <h2   style="text-align: center; style="color:blue" class="row-title"> Pour La Forme </h2>

    <p   style="text-align: center; style="color:blue">Voila la liste des regimes que vous pouvez suivre</p>




    <hr class="cmDEY eufYP cDHZq" style="color: dimgray;width: 750px;">
</header>

<footer>
    <div style="font-size: 15px;margin-left: 10px" style="color:blue">  Adresse : Esprit Technopole Ghazela Ariana</div>
    <div style="font-size: 15px;margin-left: 10px">  Numéro : +216 92141865</div>
    <div style="font-size: 15px;margin-left: 10px">  E-mail : pourlaforme@gmail.com</div>
</footer>




<h1 align="center">liste des reservations</h1>


    <table class="table">
        <thead>
        <tr>
            <th>Num Reservation</th>
            <th>User</th>
            <th>Restaurant</th>
            <th>Nombre de personne</th>
            <th>Heure</th>
            <th>Date</th>

        </tr>
        </thead>
        <tbody>
        {% for reservation in reservations %}
        <tr>
            <td>{{ reservation.idReservation }}</td>
            <td>{{ reservation.idUser }}</td>
            <td>{{ reservation.idRestaurant }}</td>
            <td>{{ reservation.idTable.typeTable }}</td>
            <td>{{ reservation.heure }}</td>
            <td>{{ reservation.date }}</td>

        {% endfor %}
        </tbody>
    </table>

{% endblock %}
